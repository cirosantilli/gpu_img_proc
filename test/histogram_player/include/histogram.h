#include <stdint.h>

void compute_histogram(unsigned int texture, unsigned int hist_obj, int width, int height);
void compute_histogram(unsigned int texture, unsigned int* hist_obj, int width, int height);

